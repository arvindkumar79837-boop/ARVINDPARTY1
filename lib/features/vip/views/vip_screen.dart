import 'package:flutter/material.dart';

class VIPScreen extends StatelessWidget {
  const VIPScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('VIP Screen - Coming Soon')),
    );
  }
}